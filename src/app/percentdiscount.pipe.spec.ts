import { PercentdiscountPipe } from './percentdiscount.pipe';

describe('PercentdiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentdiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
